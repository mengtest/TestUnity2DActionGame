namespace TestUnity2DActionGame.Domain.Common {
    // サウンド（効果音）
    public enum SE{
        MakingSound,
        OK,
        Cancel,
        Jump,
        DialogOpen
    }
    // サウンド（BGM）
    public enum BGM{
        Title,
        SelectStage,
        Stage1,
        Result
    }
}